/**
 * HTTP client for communicating with the Corredora DF API (NestJS).
 *
 * Responsibilities:
 * - Base URL configuration
 * - Default headers and credentials (httpOnly cookies)
 * - Centralized error handling
 * - Typed request/response wrappers
 */

export {};
