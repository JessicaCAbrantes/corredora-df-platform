/**
 * Browser storage abstraction.
 *
 * Responsibilities:
 * - Safe wrappers for localStorage and sessionStorage
 * - SSR-safe access (no-op on server)
 * - Typed get/set/remove operations
 */

export {};
