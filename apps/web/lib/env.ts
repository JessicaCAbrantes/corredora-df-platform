/**
 * Environment variables validation and access.
 *
 * Responsibilities:
 * - Read and validate NEXT_PUBLIC_* and server-only variables
 * - Provide typed access to env config
 * - Fail fast on missing required variables
 */

export {};
